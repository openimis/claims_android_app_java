query GetMedications($offset: Int) {
  medicalItems(offset: $offset) {
  pageInfo{
    hasNextPage
  }
    edges {
      node {
        id
        code
        name
        type
        price
      }
    }
  }
}