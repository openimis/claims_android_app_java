query GetHealthFacilities ($offset: Int){
    healthFacilities(offset: $offset) {
        pageInfo {
          hasNextPage
        }
        edges {
          node {
            id
            code
            name
          }
        }
      }
}